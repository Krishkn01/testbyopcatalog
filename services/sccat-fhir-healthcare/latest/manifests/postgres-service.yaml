apiVersion: v1
kind: Service
metadata:
  name: fhir-postgres
  labels:
    app: fhir-postgres
    component: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: fhir-postgres

# Made with Bob